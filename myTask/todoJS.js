"use strict"

let tasks = [];

function addTask()
{
    let task = {
        task: value,
        priority: value,
        status: value,
        date: value,
    };
    tasks.push(task);
    return tasks;
}

function deleteTask(tasks)
{
    tasks.splice()
}